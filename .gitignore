# dependencies
node_modules
.pnp
.pnp.js

# next.js
.next
out
next-env.d.ts

# production
build

# misc
.DS_Store
*.pem
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env
.env
.env.local
.env.*.local

# typescript
*.tsbuildinfo

# Demo video: source is tracked, media is not.
# The composition, the narration script and the package manifest are worth
# reading. The renders, the extracted screen-capture clips and the generated
# voiceover are 108MB of binary that every clone would otherwise pay for.
demo/remotion/node_modules/
demo/remotion/out/
demo/remotion/clips/
demo/remotion/vo/
demo/remotion/public/clips/
demo/remotion/public/vo/

# Agent runtime state. The key lives only in .env (already ignored); reports
# and the block cursor are produced at runtime and belong on the machine that
# produced them, not in git.
agent/reports/
agent/state.json
agent/node_modules/
